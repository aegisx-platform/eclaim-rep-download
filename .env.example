# Docker Image Version (optional - defaults to 'latest')
# Use specific version for production: v2.0.0, v2.1.0, etc.
# Leave empty or use 'latest' for newest version
VERSION=latest

# E-Claim Credentials
ECLAIM_USERNAME=your_username_here
ECLAIM_PASSWORD=your_password_here

# Database Configuration
# DB_TYPE: 'postgresql' or 'mysql'
# For PostgreSQL (docker-compose.yml):
DB_TYPE=postgresql
DB_HOST=db
DB_PORT=5432
DB_NAME=eclaim_db
DB_USER=eclaim
DB_PASSWORD=eclaim_password

# For MySQL (docker-compose-mysql.yml), use:
# DB_TYPE=mysql
# DB_HOST=db
# DB_PORT=3306
# DB_NAME=eclaim_db
# DB_USER=eclaim
# DB_PASSWORD=eclaim_password

# pgAdmin Configuration (optional, for PostgreSQL)
PGADMIN_DEFAULT_EMAIL=admin@eclaim.local
PGADMIN_DEFAULT_PASSWORD=admin

# Application Settings
FLASK_ENV=production
TZ=Asia/Bangkok
