# NHSO Revenue Intelligence - External Database Configuration
# สำหรับใช้กับ docker-compose-deploy-no-db.yml
#
# Note: docker-compose-deploy-no-db.yml ใช้ network_mode: "host"
#       Container จะใช้ network stack ของ host โดยตรง
#       สามารถเชื่อม database ที่ localhost หรือ IP ใดๆ ในเครือข่ายได้เลย

# E-Claim Credentials
ECLAIM_USERNAME=your_eclaim_username
ECLAIM_PASSWORD=your_eclaim_password

# Database Configuration (External/Host/LAN Database)
DB_TYPE=mysql                          # mysql หรือ postgresql
DB_HOST=192.168.0.218                  # IP address ของ database server
DB_PORT=3306                           # 3306 สำหรับ MySQL, 5432 สำหรับ PostgreSQL
DB_NAME=eclaim_db
DB_USER=eclaim
DB_PASSWORD=eclaim_password

# DB_HOST Examples:
# DB_HOST=localhost                    # Database บน host machine (same server)
# DB_HOST=192.168.0.218                # Database บน LAN IP
# DB_HOST=10.0.1.100                   # Database บนเครือข่ายภายใน
# DB_HOST=db.example.com               # Database hostname

# Application Settings
VERSION=latest                         # หรือระบุ version เช่น 4.0.6
FLASK_ENV=production
SECRET_KEY=your-secret-key-here        # Generate: python -c "import secrets; print(secrets.token_hex(32))"
